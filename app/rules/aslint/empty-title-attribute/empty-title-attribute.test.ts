import { EmptyTitleAttribute } from './empty-title-attribute';

describe('Rules', () => {

  describe('EmptyTitleAttribute', () => {

    it('should indicate that class exists', () => {
      expect(EmptyTitleAttribute).toBeDefined();
    });

  });
});
